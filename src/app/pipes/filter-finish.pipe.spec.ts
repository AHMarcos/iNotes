import { FilterFinishPipe } from './filter-finish.pipe';

describe('FilterFinishPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFinishPipe();
    expect(pipe).toBeTruthy();
  });
});
